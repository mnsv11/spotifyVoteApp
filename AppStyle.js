const React = require('react-native');
const {StyleSheet} = React;

export default StyleSheet.create({
    bigBlue: {
        color: 'blue',
        fontWeight: 'bold',
        fontSize: 20,
    },
    margin: {
        marginTop: 30
    },
    red: {
        color: 'red',
    },
}); 